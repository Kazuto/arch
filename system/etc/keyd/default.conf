[ids]
*

[main]
# Map Caps Lock to Hyper layer (Esc on tap, Ctrl+Shift+Alt when held)
capslock = overload(hyper, esc)

[hyper:C-S-A]
# This layer adds Ctrl+Shift+Alt to all keys
# Useful for Hyprland workspace switching and custom binds
